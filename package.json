{
  "name": "@lifespace/mcp",
  "version": "0.1.1",
  "description": "LifeSpace Platform MCP — stdio server wrapping live LSP services as first-class MCP tools for Claude Code.",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "lsp-mcp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/server.ts",
    "smoke": "tsx src/smoke.ts",
    "prepare": "tsc"
  },
  "files": [
    "dist",
    "src",
    "tsconfig.json",
    "README.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LifeSpaceLLC/LifeSpacePlatform_MCP.git"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20"
  }
}
